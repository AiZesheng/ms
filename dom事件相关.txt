1、什么是事件冒泡
事件会从最内层的元素开始发生，一直向上传播，直到document对象。
2、如果阻止事件冒泡
通过e.stopPropagation()
3、什么是事件捕获
与事件冒泡相反，事件会从最外层开始发生，直到最具体的元素。
4、事件委托
事件委托也叫事件代理，利用事件冒泡的原理将事件绑定到父元素上，而不是绑定到子元素，这样当子元素触发事件时会冒泡到父元素上。
5、事件委托的优点
(1)、对于动态添加的元素也能处理，不需要为每个元素单独绑定事件
(2)、减少内存占用，因为只给父元素绑定了事件，React的合成事件就是将所有事件都绑定到docuemnt上，从而减少了内存的开销。